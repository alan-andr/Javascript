let letters = ['a', 'b', 'c', 'd'];

let reverseLetters = [];

for(let i = letters.length - 1; i >= 0; i--) {
    reverseLetters.push(letters[i]);
}

console.log(letters);
console.log(reverseLetters);