let names = ['maria', 'joão', 'ana', 'carlos'];

const upperCaseNames = names.map(word => word.toUpperCase());

console.log(`Lista de nomes: ${names}`);
console.log('----------------------------------------');
console.log(`Lista de nomes em *maiúsculas*: ${upperCaseNames}.`);