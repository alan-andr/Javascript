const names = ['Carlos', 'Ana', 'João', 'Maria'];

const upperCaseNames = names.map(name => name.toUpperCase());

console.log(`Nomes: ${names}`);
console.log(`Nomes em maiúsculas: ${upperCaseNames}`);