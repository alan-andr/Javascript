const userProfile = {
    name: 'Alex',
    email: 'alex@example.com',
    isActive: true
};

userProfile.isActive = false;
userProfile.followers = 99;

console.log(`Mostrando usuário: `, userProfile);