let nomes = ["joão","pedro","igor","klebinho","juninho"];

for (pos in nomes) {
    console.log(`${nomes[pos]}`)
};

for(let mostrarNome = 0; mostrarNome < nomes.length; mostrarNome++) {
    console.log(`${nomes[mostrarNome]}`);
}